auth_modules:
  first:
    type: userpass
    userpass:
      username: postgres
      password: ${DB_PASSWORD}
    options:
      sslmode: require

db_url: postgresql://postgres:${DB_PASSWORD}@nozomi.proxy.rlwy.net:53581/railway